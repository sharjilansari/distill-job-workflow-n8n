{
  "name": "apply-svc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Playwright application submitter and PDF renderer, driven by n8n over HTTP.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "login": "node src/login.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "express": "^4.19.2",
    "playwright": "^1.47.0"
  }
}
